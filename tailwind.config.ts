import type { Config } from "tailwindcss";

export default {
  darkMode: ["class"],
  content: [
    "./pages/**/*.{ts,tsx}",
    "./components/**/*.{ts,tsx}",
    "./app/**/*.{ts,tsx}",
    "./src/**/*.{ts,tsx}",
  ],
  prefix: "",
  theme: {
    container: {
      center: true,
      padding: '2rem',
      screens: {
        '2xl': '1400px'
      }
    },
    extend: {
      colors: {
        // Dominant yellow and white color scheme
        background: '#FFFFFF',
        foreground: '#333333',
        primary: {
          DEFAULT: '#FFC700', // Vibrant yellow
          foreground: '#000000'
        },
        secondary: {
          DEFAULT: '#FEF7CD', // Soft pastel yellow
          foreground: '#333333'
        },
        accent: {
          DEFAULT: '#FFED4A', // Bright yellow
          foreground: '#000000'
        },
        border: '#E2D1C3', // Soft beige border
        input: '#FFC700',
        ring: '#FFC700'
      },
      borderRadius: {
        lg: 'var(--radius)',
        md: 'calc(var(--radius) - 2px)',
        sm: 'calc(var(--radius) - 4px)'
      },
      keyframes: {
        'accordion-down': {
          from: {
            height: '0'
          },
          to: {
            height: 'var(--radix-accordion-content-height)'
          }
        },
        'accordion-up': {
          from: {
            height: 'var(--radix-accordion-content-height)'
          },
          to: {
            height: '0'
          }
        }
      },
      animation: {
        'accordion-down': 'accordion-down 0.2s ease-out',
        'accordion-up': 'accordion-up 0.2s ease-out'
      }
    }
  },
  plugins: [require("tailwindcss-animate")],
} satisfies Config;
